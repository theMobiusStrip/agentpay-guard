{
  "name": "@agentpay-guard/drainbench",
  "private": true,
  "version": "0.0.1",
  "description": "DrainBench — reproducible adversarial harm-metric benchmark for agentpay-guard. Deterministic arm-A (native hook) vs arm-B (native + guard) over a mock settlement ledger; four-metric oracle.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "bench": "tsx src/cli.ts",
    "agent-demo": "tsx src/agent/demo.ts"
  },
  "dependencies": {
    "@themobiusstrip/agentpay-guard": "0.0.1",
    "@themobiusstrip/x402-idempotency-middleware": "0.0.1",
    "@x402/core": "2.17.0",
    "@x402/evm": "2.17.0",
    "@x402/fetch": "2.17.0",
    "viem": "2.31.7"
  },
  "license": "MIT"
}
